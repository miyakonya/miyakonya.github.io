---
title: Web安全基础Part2-SQL注入
date: 2026-07-26 23:55:08 +0800
categories: [网络安全]
tags: [网络安全, Web安全]
---

# Web安全基础-Part2-SQL注入

> **警告**: 本实验仅在本地授权环境中进行，请勿在未授权网络中尝试相关攻击行为。
{: .prompt-warning }

## 一、什么是SQL注入(SQL Injection)
SQL注入(SQL Injection)是指应用程序将用户输入直接拼接到SQL语句中执行，攻击者可以构造特殊输入改变SQL的原本语义，从而绕过身份验证、读取、修改甚至删除数据库中的数据。
常见的sql注入攻击方式有：
- 联合查询(Union Query)
- 布尔盲注(Boolean Blind Injection)
- 时间盲注(Time Blind Injection)
- 堆叠注入(Stacked Injection)
- 报错注入(Error-based SQL Injection)

---

## 二、环境搭建
了解完sql注入是什么，我们开始搭建环境。

### 2.1 前端环境搭建

前端html网页源码(templates/login.html)如下：

```html
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>登录</title>
</head>
<body>
<h2>登录你的账号</h2>
<form method="post" action="http://127.0.0.1:5000/login">
    用户名:<input type="text" name="username">
    <br><br>
    密码:<input type="password" name="password">
    <br><br>
    <button type="submit">登录</button>
</form>
</body>
</html>
```

### 2.2 后端环境搭建

接下来写一下后端代码：
```py
# coding: UTF-8
# Python 3.10.6

from flask import *
from db import getCursor

app = Flask(__name__)

@app.route("/")
def index():
    return "Hello From Flask!"

# 如果是GET请求，则转到登录网页
@app.route("/login", methods=["GET"])
def login_page():
    return render_template("login.html")

# 接收到POST请求，开始执行登录代码
@app.route("/login", methods=["POST"])
def login():
    cursor = getCursor()    # 获取sql连接游标
    username = request.form.get("username") # 获取前端传来的用户名
    password = request.form.get("password") # 获取前端传来的密码
    # 将用户名和密码拼接到sql语句中(不安全)
    sql = "SELECT * FROM users WHERE username='{}' AND password='{}'".format(username, password)
    # 执行
    cursor.execute(sql)
    print("sql语句执行:", sql)
    if cursor.fetchone():
        return "<h1>欢迎用户{}登录</h1>".format(username)
    else:
        return "<h1>登录失败</h1>"

if __name__ == "__main__":
    app.run()
```

创建mysql数据库连接器：

```py
# coding: UTF-8
# Python 3.10.6

import pymysql

def getCursor():
    # 下列信息根据实际填写，教学推荐用root用户
    pms = pymysql.connect(host="localhost",
                          port=3306,
                          user="",
                          password="",
                          database="webtest")
    return pms.cursor()
```


### 2.3 数据库环境搭建
进入mysql命令行，进行环境搭建：
```sql
CREATE DATABASE webtest CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci; -- 创建一个名为webtest的数据库，编码用utf-8
use webtest; -- 切换到新创建的数据库
CREATE TABLE IF NOT EXISTS users (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '用户ID，主键',
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名，不能为空且唯一',
    password VARCHAR(255) NOT NULL COMMENT '密码，不能为空'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表'; -- 创建表
-- 创建几个示例数据
INSERT INTO users (id, username, password) VALUES (NULL, "admin", "123");
INSERT INTO users (id, username, password) VALUES (NULL, "root", "root");
INSERT INTO users (id, username, password) VALUES (NULL, "Mike", "mk123");
```
需要注意的是，密码列通常不会明文存储，而是进行哈希处理，这里为了便于演示用明文存储。

---

## 三、漏洞演示
现在，运行后端服务器，并打开登录网页，输入一个正确用户名和密码进行测试：
![](/assets/img/sql/sql1.png)

登录成功。接着，返回登录页面，构造一个恶意的用户名(例如`admin'-- `)，密码任意(***注意：这里的用户名最后还有个空格，这是为了符合sql语法***)：

![](/assets/img/sql/sql2.png)

可以发现，即便密码错误，也能够登录。
![](/assets/img/sql/sql3.png)

接下来我们看看后端是如何操作的：
![](/assets/img/sql/sql4.png)
根据这个sql语句，我们可以判断出，在用户名后的一个单引号将username参数给闭合掉了，因此后面的数据被当成了sql语句，而`--`在sql中是注释符(其后要加一个空格)， 它把`--`后的代码被注释掉了，不会执行。所以实际执行的sql语句为：
```sql
SELECT * FROM users WHERE username='admin';
```
由于admin用户肯定存在的，所以这条语句恒成立，绕过了密码验证，直接登录了。

冷知识：类似`admin'-- `、`' OR '1'='1`等语句在以前网络还不够安全的年代，称为“万能密码”。它们都是修改了`WHERE`条件，使得原sql表达式恒成立从而绕过认证。

---

## 四、修复
根据sql注入原理，我们可以得知，这个漏洞的本质是***代码与用户数据边界混淆***，导致用户数据被当成了sql代码的一部分，所以，只要让应用程序把它们的边界分清，那么就可以防御了。
目前，主流的修复sql注入漏洞的方式为使用“参数化查询(Parameterized Query)”。
参数化查询适用于sql语句(`SELECT`、`UPDATE`、`INSERT`等)中的数据参数，但不能用于表名，列名等sql标识符。
参数化查询的原理是把用户参数整体当成数据去处理，这就避免了代码与用户数据边界混淆。

在`Python`中，可以通过下列方式实现参数化查询：
```py
# 这里的占位符必须用%s，即便它是整数型
sql = "SELECT * FROM users WHERE username=%s AND password=%s"
cursor.execute(sql, (username, password))   # 如果只有一个参数，则应写为cursor.execute(sql, (username,)) 
```

在`Java`中，可以用“预编译语句(Prepared Statement)”。
```java
String sql = "SELECT * FROM users WHERE username=? AND password=?";
try (PreparedStatement pstmt = conn.prepareStatement(sql)) {
    pstmt.setString(1, username);
    pstmt.setString(2, password);
    ResultSet rs = pstmt.executeQuery();
}
```

在我们上面的python例子中，可以这么写：
```py
sql = "SELECT * FROM users WHERE username=%s AND password=%s"
cursor.execute(sql, (username, password))
print("sql语句执行:", sql % (username, password))
"""
注意！！！
这里print()语句为了打印sql语句，用的是sql % (username, password)
这并不是参数化查询，cursor.execute(sql, (username, password))才是
如果用cursor.execute(sql % (username, password))的话仍然会导致sql注入！
"""
```

接着，重复刚刚的攻击，会发现登录失败，此时查看后端执行的sql语句：
![](/assets/img/sql/sql5.png)

可以看到，`admin'-- `整体被当作用户名去查询了，后面的sql语句也正常执行，防御了sql注入。